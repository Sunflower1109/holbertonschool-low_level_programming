0. create_array - creates an array of chars, and initializes it with a specific char.
1. _strdup - returns a pointer to a newly allocated space in memory, which contains a copy of the string given as a parameter.
2. str_concat - concatenates two strings and returns pointer to a newly allocated space in memory containing s1 followed by s2.
3. alloc_grid - returns a pointer to a 2D array of integers.
4.  function that frees a 2 dimensional grid previously created by your alloc_grid function.
